<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:jee="http://www.springframework.org/schema/jee" xmlns:tx="http://www.springframework.org/schema/tx"
	xmlns:context="http://www.springframework.org/schema/context" xmlns:aop="http://www.springframework.org/schema/aop"
	xsi:schemaLocation="http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans-3.1.xsd
	http://www.springframework.org/schema/tx http://www.springframework.org/schema/tx/spring-tx-3.1.xsd
	http://www.springframework.org/schema/jee http://www.springframework.org/schema/jee/spring-jee-3.1.xsd
	http://www.springframework.org/schema/aop http://www.springframework.org/schema/aop/spring-aop-3.1.xsd
	http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-3.1.xsd">

	<!-- Root Context: defines shared resources visible to all other web components -->

	<description>Spring</description>

	<!-- 使用annotation 自动注册bean,并检查@Required,@Autowired的属性已被注入 -->
	<!-- 不扫描@Controller, 交由spring mvc进行扫描 -->
	<context:component-scan base-package="com.shuodao.paigua">
		<context:exclude-filter type="annotation" expression="org.springframework.stereotype.Controller" />
		<!-- 
		<context:exclude-filter type="aspectj" expression="com.rongzhuo.weixin.db.entity..*" />
		 -->
		 
	</context:component-scan>

	<!--
	<bean class="com.yy.tuna.admin.TunaAdminBootstrap" lazy-init="false" >
		<property name="development" value="${is.development}" />
		<property name="envMark" value="${env.mark}" />
		<property name="staticFileVersion" value="${static.file.version}" />
	</bean>
	-->
	
	<!-- 以静态变量保存ApplicationContext 
	<bean class="com.yy.tuna.core.util.SpringContextHolder" lazy-init="false" />
-->
	<!-- 利用zookeeper里的jdbc配置实例化dataSource -->
	<bean id="dataSource" class="com.shuodao.paigua.config.DataSourceFactoryBean"></bean>

	<!-- MyBatis配置--> 
	<bean id="sqlSessionFactory" class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="classpath:mybatis-config.xml" />
		<property name="mapperLocations" value="classpath:/mapper/*Mapper.xml" />
		<property name="typeAliasesPackage" value="com.shuodao.paigua.db.entity" />
	</bean>

	<bean class="org.mybatis.spring.mapper.MapperScannerConfigurer">
		<property name="basePackage" value="com.shuodao.paigua.db.mapper" />
	</bean>

	<bean id="transactionManager" class="org.springframework.jdbc.datasource.DataSourceTransactionManager">
		<property name="dataSource" ref="dataSource" />
	</bean>
	
	<!-- 使用annotation定义事务 -->
	<tx:annotation-driven transaction-manager="transactionManager" proxy-target-class="true" />

	<!-- freemarker config -->
	<bean id="freemarkerConfig" class="org.springframework.web.servlet.view.freemarker.FreeMarkerConfigurer">
		<property name="templateLoaderPath" value="/WEB-INF/templates/" />
		<property name="freemarkerSettings">
			<props>
				<prop key="defaultEncoding">UTF-8</prop>
			</props>
		</property>
	</bean>
	<!-- 
	<bean id="ftlShortStringFunction" class="com.yy.mango.freemarker.ShortStringFunction" />
	<bean id="ftlCDNFunction" class="com.yy.tuna.core.util.FtlCDNFunction" />
	 -->

	<!-- 不同的环境使用不同的配置(默认为: 开发环境) -->
	<!-- 开发环境 -->
	<!-- <beans profile="development">
		<bean class="org.springframework.beans.factory.config.PropertyPlaceholderConfigurer">
			<property name="systemPropertiesModeName" value="SYSTEM_PROPERTIES_MODE_OVERRIDE" />
			<property name="ignoreResourceNotFound" value="true" />
			<property name="locations">
				<list>
					标准配置
					<value>classpath*:/application.properties</value>
					本地开发环境配置
					<value>classpath*:/application.local.properties</value>
				</list>
			</property>
		</bean> -->
		
	</beans>
